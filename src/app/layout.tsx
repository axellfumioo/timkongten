"use client"

import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SessionProvider } from "next-auth/react";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Pindahkan metadata ke komponen terpisah atau gunakan generateMetadata
// export const metadata: Metadata = {
//   title: "Tim Konten Dashboard",
//   description: "Dashboard untuk tim konten SMK Telkom Purwokerto",
// };

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <title>Tim Konten Dashboard</title>
        <meta name="description" content="Dashboard untuk tim konten SMK Telkom Purwokerto" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <SessionProvider>
          {children}
        </SessionProvider>
      </body>
    </html>
  );
}
